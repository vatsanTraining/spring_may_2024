spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/training 
    username: postgres
    password: srivatsan
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      
server:
  port: 4040
        
   