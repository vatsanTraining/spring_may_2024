spring:
  application:
    name: spring-mvc
  datasource:
    url: jdbc:postgresql://localhost:5432/training 
    username: postgres
    password: srivatsan
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
   